# CLAUDE.md

## gstack

Use the `/browse` skill from gstack for all web browsing. Never use `mcp__claude-in-chrome__*` tools directly.

Available gstack skills:

- `/office-hours` — structured async review with stakeholders
- `/plan-ceo-review` — CEO-level plan review
- `/plan-eng-review` — engineering plan review
- `/plan-design-review` — design plan review
- `/design-consultation` — design consultation and proposals
- `/design-shotgun` — rapid multi-direction design exploration
- `/design-html` — generate HTML/CSS design artifacts
- `/review` — code review
- `/ship` — ship a PR end-to-end
- `/land-and-deploy` — land and deploy a change
- `/canary` — canary deploy and monitor
- `/benchmark` — run and compare benchmarks
- `/browse` — web browsing (use this for all browsing tasks)
- `/connect-chrome` — connect to a Chrome browser session
- `/qa` — full QA pass
- `/qa-only` — QA without code changes
- `/design-review` — design review pass
- `/setup-browser-cookies` — set up browser cookies for auth
- `/setup-deploy` — configure deployment settings
- `/setup-gbrain` — configure gbrain integration
- `/retro` — run a retrospective
- `/investigate` — deep investigation of an issue
- `/document-release` — generate release documentation
- `/document-generate` — generate documentation
- `/codex` — codex-style task execution
- `/cso` — chief security officer security audit
- `/autoplan` — automatically generate an implementation plan
- `/plan-devex-review` — developer experience plan review
- `/devex-review` — developer experience review
- `/careful` — careful/conservative mode for risky changes
- `/freeze` — freeze a branch or feature
- `/guard` — guard against regressions
- `/unfreeze` — unfreeze a branch or feature
- `/gstack-upgrade` — upgrade gstack to latest
- `/learn` — learn about a codebase or topic
